{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: 实验一修复版",
            "type": "debugpy",
            "request": "launch",
            // 既然已经在 myproj 根目录了，直接指向 src
            "program": "${workspaceFolder}/src/main.py",
            "console": "integratedTerminal",
            // 直接就在当前工作区运行
            "cwd": "${workspaceFolder}", 
            "args": ["test.jpg"],
            "justMyCode": true
        }
    ]
}